meta {
  type: collection
}

headers {
  Accept-Language: {{accept_language}}
  User-Agent: {{user_agent}}
}

auth {
  mode: none
}

docs {
  CLI-focused subset of requests extracted from the captured browser fetch log.
  Static assets and third-party anti-bot traffic were intentionally excluded.
}
